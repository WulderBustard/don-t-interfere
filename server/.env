PORT=3001
DB_FILE=./db/data.sqlite
JWT_SECRET=wdawjgawd;k

# Let's Encrypt certificate paths. Works only with a certificate issued for the
# exact host/IP used in the browser. Private LAN IPs like 10.21.3.86 cannot get
# a public Let's Encrypt certificate.
# SSL_KEY_PATH=C:/Certbot/live/example.com/privkey.pem
# SSL_CERT_PATH=C:/Certbot/live/example.com/fullchain.pem
